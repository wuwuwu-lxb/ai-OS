[package]
name = "ai-os-kernel"
version = "0.1.0"
edition = "2021"

[workspace]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
codegen-units = 1

[profile.release.build-args]
"-C" = "link-arg=-no-pie"
